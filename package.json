{
  "name": "sample-api",
  "version": "1.0.0",
  "description": "sample serverless api",
  "author": "axlrommel",
  "license": "MIT",
  "scripts": {
    "deploy": "npm run lint && npx serverless deploy -s prod",
    "remove": "npx serverless remove -s prod"
  },
  "devDependencies": {
    "serverless": "^3.22.0"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.3"
  }
}
