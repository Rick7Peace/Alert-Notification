{
  "name": "sample-api",
  "version": "1.0.0",
  "type": "module",
  "description": "sample serverless api",
  "author": "axlrommel",
  "license": "MIT",
  "scripts": {
    "deploy": "npx serverless deploy -s prod",
    "remove": "npx serverless remove -s prod",
    "lint": "eslint --ignore-path .gitignore ."

  },
  "devDependencies": {
    "@serverless/eslint-config": "^5.0.1",
    "eslint": "^8.23.1",
    "eslint-plugin-import": "^2.26.0",
    "serverless": "^3.22.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
